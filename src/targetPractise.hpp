#ifndef TARGETPRACTISE_H
#define TARGETPRACTISE_H

class TargetPractise {
#define SCREENWIDTH 1920
#define SCREENHEIGHT 1080
#define PLAYERPOSITION (Vector3){0.00000000001,1,0.00000000001} // cannot be zero, otherwise the camera stops existing or something of the sort.
#define DEBUG 1
private:
public:
};

#endif
